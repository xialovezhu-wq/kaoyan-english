# 不可写正式数据清单

更新时间：2026-06-28

本清单用于防止 LAMBIC 架构搭建、验收、Tutor 接入和第二大脑同步时污染英语正式源数据。

## 本目标期间默认只读

| 路径 | 原因 | 允许动作 |
|---|---|---|
| `bank/master_bank.csv` | 长期结构化总库，13 列固定；用户未要求精选入库 | 只读查询、只读 lint 报告 |
| `review/weekly_review.md` | 周复盘只在用户明确要求时更新 | 只读检查 |
| 既有 `articles/*.md` | 正式文章精读资产，默认不得被批量重建覆盖 | 只读检索；明确的文章 intake 可新增目标页，本次保留 2010 Text 4 与 2011 Text 1–4 既有学习页 |
| `review/tomorrow_review.md` | 临时复习清单，不应被框架搭建顺手改写 | 只读 lint、边界说明 |
| `review/daily_review_log.md` | 每日复盘日志，不应由架构任务补写学习记录 | 只读查询 |
| `bank/mastered_items.csv` | 已掌握排除池，只能由明确主动使用证据触发 | 只读查询 |
| `bank/sentence_patterns.md` | 句式卡正式库，新增/归并需明确句式入库任务 | 只读查询、作为 Tutor 候选来源引用 |

## 可安全新增或更新

| 路径 | 用途 |
|---|---|
| `schema/` | 规则、操作流程、lint 边界 |
| `wiki/index.md` | 英语 wiki 总入口 |
| `wiki/log.md` | 框架、验收、同步日志 |
| `wiki/operation_center/` | 智能体操作中心、看板、规则清单 |
| `wiki/workflows/` | ingest/query/lint 可复制操作说明 |
| `wiki/validation/` | 验收记录、只读测试结果 |
| `wiki/tutor/` | Tutor 接入方案 |
| `wiki/study_vaults/` | StudyVault 输入材料和专题设计 |
| `wiki/sync/` | 第二大脑同步包 |
| `raw/` | 原始来源登记和占位说明 |

## Lint 发现但不自动修复

- `bank/master_bank.csv` 中存在 1 条规则外 `type=动词`：`20260607-070 demand`。
- 重复 item 候选：`impressive`、`response`、`value`。
- 以上只写入 `wiki/lint_report.md`，不自动合并、不改字段。
